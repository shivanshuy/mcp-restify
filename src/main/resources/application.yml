server:
  port: 9092

spring:
  application:
    name: mcp-restify

# Outlook/Microsoft Graph Configuration
outlook:
  client-id: ${OUTLOOK_CLIENT_ID:}
  client-secret: ${OUTLOOK_CLIENT_SECRET:}
  tenant-id: ${OUTLOOK_TENANT_ID:}

logging:
  level:
    com.restify.mcp: DEBUG
    org.springframework: INFO

